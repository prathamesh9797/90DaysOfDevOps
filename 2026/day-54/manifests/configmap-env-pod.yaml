apiVersion: v1
kind: Pod
metadata:
  name: configmap-env-pod
spec:
  containers:
    - name: busybox
      image: busybox
      command: ["sh", "-c", "env | grep APP; sleep 3600"]
      envFrom:
        - configMapRef:
            name: app-config